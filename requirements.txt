# ========== Core Dependencies (Required) ==========
requests>=2.31.0         # HTTP requests for all APIs
python-dotenv>=1.0.0     # .env file management
python-whois>=0.8.0      # WHOIS domain lookups
dnspython>=2.4.0         # DNS record analysis

# ========== Caching (v3.4 - Recommended for production) ==========
redis>=5.0.0             # Redis caching layer
redis[asyncio]>=5.0.0    # Redis async support for streaming (v4.0)

# ========== REST API (v3.5 - API Service) ==========
fastapi>=0.109.0         # Modern web framework
uvicorn[standard]>=0.27.0  # ASGI server
pydantic>=2.0.0          # Data validation and schemas
python-multipart>=0.0.6  # Form data parsing

# ========== Optional (Production) ==========
# gunicorn>=21.2.0       # Production WSGI server
# celery>=5.3.0          # Async batch processing
# pytest>=7.4.0          # Testing framework
# httpx>=0.25.0          # Async HTTP client
